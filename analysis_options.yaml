# analysis_options.yaml
include: package:flutter_lints/flutter.yaml

analyzer:
  enable-experiment:
    - dot-shorthands  # Enable this experimental feature

linter:
  rules:
    # Your linter rules here